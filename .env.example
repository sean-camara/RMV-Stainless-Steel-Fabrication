# FRONTEND-ONLY .env example for RMV Frontend
# Copy this file to .env and fill in the required values for the frontend only.
# Do NOT copy or reuse the repository root `.env.example` — that file contains
# backend-only settings (MongoDB, JWT, SMTP) and is not required to run the frontend.

# Base URL for backend API (required)
VITE_API_URL=http://localhost:5000/api

# (Optional) Public URL for frontend
# FRONTEND_URL=http://localhost:5173

# (Optional) Node environment
# NODE_ENV=development
